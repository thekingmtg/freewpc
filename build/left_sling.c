
/* This file was autogenerated by 'ctemp' from  for class sling+ spsol. */


#include <freewpc.h>
#include <left_sling.h>

/** We keep two bitarrays to track all special solenoids.
 * The 'enables' are software controlled and allow the
 * solenoids to be disabled.  The 'running' entries say
 * for each coil whether it is in its on or off phase. */

__fastram__ U8 sling_enables;
__fastram__ U8 sling_running;

/* The time until the current phase expires */
__fastram__ U8 left_sling_timer;

/* Enable the device */
void left_sling_enable (void)
{
	sling_enables |= (1 << 0);
}

/* Disable the device */
void left_sling_disable (void)
{
	sling_enables &= ~(1 << 0);
}

/* Initialize the device */
CALLSET_ENTRY (left_sling, init)
{
	left_sling_timer = 0;
	left_sling_disable ();
}

/* Enable the device at start ball */
CALLSET_ENTRY (left_sling, start_ball)
{
#if 1
	left_sling_enable ();
#endif
}

/* Disable the device at end ball/stop game/tilt */
CALLSET_ENTRY (left_sling, end_ball, stop_game, tilt)
{
	left_sling_disable ();
}

/* vim: set filetype=c: */
