
/* This file was autogenerated by 'ctemp' from  for class jet+ spsol. */


#include <freewpc.h>
#include <top_sling.h>

/** We keep two bitarrays to track all special solenoids.
 * The 'enables' are software controlled and allow the
 * solenoids to be disabled.  The 'running' entries say
 * for each coil whether it is in its on or off phase. */


/* The time until the current phase expires */
__fastram__ U8 top_sling_timer;

/* Enable the device */
void top_sling_enable (void)
{
	jet_enables |= (1 << 2);
}

/* Disable the device */
void top_sling_disable (void)
{
	jet_enables &= ~(1 << 2);
}

/* Initialize the device */
CALLSET_ENTRY (top_sling, init)
{
	top_sling_timer = 0;
	top_sling_disable ();
}

/* Enable the device at start ball */
CALLSET_ENTRY (top_sling, start_ball)
{
#if 1
	top_sling_enable ();
#endif
}

/* Disable the device at end ball/stop game/tilt */
CALLSET_ENTRY (top_sling, end_ball, stop_game, tilt)
{
	top_sling_disable ();
}

/* vim: set filetype=c: */
